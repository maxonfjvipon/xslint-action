# SPDX-FileCopyrightText: Copyright (c) 2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
name: 'xslint-action'
description: 'Xslint linter GitHub action'
branding:
  icon: alert-triangle
  color: orange
inputs:
  args:
    description: 'Files and directories to check'
    required: true
    default: '.'
  suppress:
    description: 'Checks to suppress'
    required: false
runs:
  using: 'docker'
  image: Dockerfile
  args:
    - "${{ inputs.args }}"
    - "${{ inputs.suppress }}"
